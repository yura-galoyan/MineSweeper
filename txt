
    switch(key){
    case CURSOR::action::open:
         cursor.demine(ij,matrix[ij.first][ij.second].value  );
         reveal(matrix,ij.first,ij.second);
         break;
    case CURSOR::action::flag:
         cursor.flag(matrix[ij.first][ij.second].isOpened);
         break;
    }
    